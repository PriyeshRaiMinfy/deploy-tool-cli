apiVersion: 1

providers:
  - name: 'ecs_dashboards'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards


# cat > /mnt/efs/grafana/provisioning/dashboards/dashboard.yml << 'EOF'
# apiVersion: 1

# providers:
#   - name: 'default'
#     orgId: 1
#     folder: ''
#     type: file
#     disableDeletion: false
#     updateIntervalSeconds: 10
#     allowUiUpdates: true
#     options:
#       path: /mnt/efs/grafana/provisioning/dashboards
# EOF